# Tugas Akhir - Klasifikasi Sentimen Solar Home System (Hybrid NLP)
# Tahap fondasi data: DatasetService + PreprocessingService.
# Catatan: TIDAK menggunakan pandas (seluruh operasi DataFrame via Polars).

polars>=1.41
numpy>=1.26
matplotlib>=3.8
torch>=2.2
transformers>=4.40
accelerate>=0.29
scikit-learn>=1.4
safetensors>=0.4
pyarrow>=15.0
scipy>=1.12
